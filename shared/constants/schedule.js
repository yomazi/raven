// Enum for schedule.releaseMode — shared by client and server.

export const RELEASE_MODES = ["asap", "on-schedule", "tbd"];

export const RELEASE_MODE_LABELS = {
  asap: "ASAP",
  "on-schedule": "On Schedule",
  tbd: "TBD",
};
